<%= form_for([@invite, @invite.agendas.build]) do |f| %>

  <p>
    <%= f.label :name %> <br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :time %> <br>
    <%= f.text_field :time %>
  </p>

  <%= f.submit %>

<% end %>
